import React from "react";

const ShowModuleName = (props) => {


  return (
    <div className="module-name">
      <h1>{props.name}</h1>
      <p>Module</p>
    </div>
  );
};

export default ShowModuleName;
